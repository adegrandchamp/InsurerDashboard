# you shouldn't commit these into source control
# these are the default directory names, adjust/add to fit your needs
target/
dbt_packages/
logs/
# the model is large so not uploading to git
data_modeling/jessica_work/rsf_trained_model.pkl
data_modeling/jessica_work/__pycache__/